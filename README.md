This ia a movies API
  this API helps us to fetchthe list of movies 
  the db used here is mongo and crud operations can be carried out


The routes in this app are

GET /movies 
POST /movies
GET /movies/{movie_id}
PUT /movies/{movie_id}
DELETE /tasks/{task_id}